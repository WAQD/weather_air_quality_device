<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is only allowed for user-configured LAN device URLs
  (the WAQD station runs plain HTTP on the local network). All other
  traffic must use HTTPS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
