name: Build waqd_website Docker Image

on:
  push:
    branches: [master]
    paths:
      - "src/waqd_website/**"
      - "src/waqd/**"
      - "src/waqd_installer/**"
      - "src/waqd_assets/**"
      - "test/**"
      - "pyproject.toml"
      - "pdm.lock"
      - ".github/workflows/website.yml"
    tags:
      - "v*"
  pull_request:
    branches: [master]
    paths:
      - "src/waqd_website/**"
      - "src/waqd/**"
      - "src/waqd_installer/**"
      - "src/waqd_assets/**"
      - "test/**"
      - "pyproject.toml"
      - "pdm.lock"
      - ".github/workflows/website.yml"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: src/waqd_website/ui/package-lock.json

      - name: Install dependencies
        working-directory: src/waqd_website/ui
        run: npm ci

      - name: Run unit tests
        working-directory: src/waqd_website/ui
        run: npm run test:unit

  test-python:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Cache PDM dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/pdm
            .venv
          key: pdm-${{ runner.os }}-${{ hashFiles('pdm.lock') }}
          restore-keys: |
            pdm-${{ runner.os }}-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pdm
          pdm install --frozen-lockfile -G test

      - name: Run basics tests
        run: PYTHONPATH=src pdm run pytest test/basics -q

  build:
    needs: [test, test-python]
    runs-on: ubuntu-latest
    outputs:
      version_comment: ${{ steps.set_tag.outputs.version_comment }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set version comment
        id: set_tag
        run: |
          if [[ $GITHUB_REF == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}
          else
            VERSION=$(git rev-parse --short HEAD)
          fi
          echo "version_comment=# Version: ${VERSION}" >> $GITHUB_OUTPUT

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Get package.json version
        id: package_version
        run: |
          VERSION=$(node -p "require('./src/waqd_website/ui/package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
        with:
          driver: docker-container

      - name: Build Docker image
        id: build_image
        run: |
          VCS_REF=$(git rev-parse --short=6 HEAD)
          IMAGE_TAG="v${{ steps.package_version.outputs.version }}-${VCS_REF}"
          echo "image_tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT
          TAGS="-t goszpeti/waqd_website:latest -t goszpeti/waqd_website:${IMAGE_TAG}"
          docker buildx build \
            --platform linux/amd64 \
            --cache-from=type=gha \
            --cache-to=type=gha,mode=max \
            --build-arg BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ") \
            --build-arg VCS_REF=$VCS_REF \
            --build-arg VERSION=$(git describe --tags || echo "dev") \
            $TAGS \
            --load \
            -f src/waqd_website/Dockerfile \
            .

      - name: Push Docker image
        run: |
          echo ${{ secrets.DOCKER_ACCESS_TOKEN }} | docker login -u goszpeti --password-stdin
          docker push goszpeti/waqd_website:latest
          docker push goszpeti/waqd_website:${{ steps.build_image.outputs.image_tag }}

  # update-image-tag:
  #   # TODO: later; only on tagged releases if: startsWith(github.ref, 'refs/tags/')
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v3
  #       with:
  #         fetch-depth: 1
  #         repository: 'WAQD/waqd_infrastructure'
  #         token: ${{ secrets.WAQD_GITHUB_ACCESS_TOKEN }}
  #         ref: 'main'

  #     - name: Update version comment in yaml
  #       run: |
  #         IMAGE_NAME="waqd_website"
  #         # Add version comment and keep latest tag
  #         sed -i "s|image: goszpeti/$IMAGE_NAME:.*|${{ needs.build.outputs.version_comment }}\n    image: goszpeti/$IMAGE_NAME:latest|g" deployment/waqd-website.yaml

  #     - name: Commit and push changes
  #       run: |
  #         git config user.name "github-actions"
  #         git config user.email "github-actions[bot]@users.noreply.github.com"
  #         git add deployment/waqd-website.yaml
  #         git commit -m "Update version comment to ${{ needs.build.outputs.version_comment }}" || echo "No changes to commit"
  #         git push
